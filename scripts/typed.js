document.addEventListener("DOMContentLoaded", function () {
  var typing = new Typed(".text", {
    strings: [
      // here we can add more strings if we want!
      "advancing AI knowledge.",
      "fostering innovation.",
      "connecting enthusiasts.",
    ],
    typeSpeed: 50,
    backSpeed: 40,
    loop: true,
    showCursor: false,
  });
});
